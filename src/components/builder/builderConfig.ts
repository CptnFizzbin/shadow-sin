import { SystemValues } from "#/system/systemValues.ts"

/**
 * @deprecated Use `SystemValues.builder` (`#/system/systemValues.ts`) directly. Kept as an
 * alias so existing call sites keep working until they're migrated.
 */
export const BuilderConfig = SystemValues.builder
