# src/routes — routing

File-based routing via TanStack Router: add a route by creating a file in this directory. **Never edit
`src/routeTree.gen.ts`** — the Vite plugin regenerates it from these files on `yarn dev`/`yarn build`.
